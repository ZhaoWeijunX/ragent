# 活跃分支相对上游快照

- 统计时间：2026-08-05
- 上游基线：`upstream/main` `17eaaa6f`（2026-08-04）
- 统计口径：`git rev-list --left-right --count upstream/main...<branch>`；数量包含 merge 提交。

本文只描述已提交的 Git 状态，不包含当前工作区未提交的文档整理。

## 当前状态

| 分支 | 对应远程 | 当前提交 | 相对 `upstream/main` | 上游关系 | 当前定位 |
|---|---|---|---|---|---|
| `main` | `origin/main` | `c44b8a6a` | 落后 0 / 领先 25 | `upstream/main` 是祖先 | 本地稳定基线，保留部分配置与历史功能取舍 |
| `preview` | `origin/preview` | `86b1a07d` | 落后 0 / 领先 84 | `upstream/main` 是祖先 | 个人功能集成线，承载飞书、MCP、知识库与前端增强 |
| `feat/eval_workbench_v1.0` | `origin/feat/eval_workbench_v1.0` | `8332f6ad` | 落后 0 / 领先 98 | `upstream/main` 是祖先 | 当前活跃开发线，在 `preview` 能力基础上完成 RAG 评测工作台 |

三条活跃分支都已包含 `upstream/main` 的 `17eaaa6f`，因此当前没有待吸收的上游提交；领先数反映各分支保留的本地提交与合并历史，不能直接等同于独立功能数量。

```text
upstream/main (17eaaa6f)
├── main (c44b8a6a, +25)
├── preview (86b1a07d, +84)
└── feat/eval_workbench_v1.0 (8332f6ad, +98)
```

## `main`：稳定基线

- 最新状态：2026-08-05 合并 `upstream/main`，当前与上游无落后。
- 分支独有的非 merge 提交共 12 个，主要是本地配置、个人设置、Web Search 配置说明，以及对天气 MCP、部分意图文档和历史摘要的 revert。
- 使用场景：作为较保守的本地基线；不应假定其中包含 `preview` 或评测工作台的全部功能。

近期分支独有提交：

| 提交 | 日期 | 说明 |
|---|---|---|
| `1d73fa58` | 2026-07-16 | 增加 Web Search API Key 配置占位说明 |
| `0ec5c900` | 2026-07-07 | 回退历史分支摘要文档 |
| `c8ced879` | 2026-07-07 | 回退 ragent-test 意图树文档与导入 |
| `90f9b955` | 2026-07-07 | 回退天气 MCP 功能 |

## `preview`：功能集成线

- 最新状态：2026-08-05 合并 `upstream/main`，当前与上游无落后。
- 分支独有的非 merge 提交共 60 个，汇集了飞书、知识库、MCP、模型与前端增强。
- 功能范围：飞书 Wiki / Docx Remote URL 与批量导入、PDF 默认导出和降级链；销售 / 工单 PG MCP、天气 MCP、You.com 搜索；知识库样本与意图树；模型健康探测、消息重新生成与界面增强。
- 使用场景：验证个人功能的综合兼容性；后续同步上游可继续在此处理集成冲突。

近期分支独有提交：

| 提交 | 日期 | 说明 |
|---|---|---|
| `493e91ab` | 2026-07-29 | SYSTEM 意图评测旁路，避免强制全局检索导致结果虚高 |
| `ba2dfa05` | 2026-07-28 | 优化知识分块启用 / 禁用的并发安全与幂等性 |
| `c39bfd7e` | 2026-07-28 | 增加 12306 知识库文档与意图树 |
| `dfee674d` | 2026-07-28 | 增加 12306 PDF 批量导入脚本 |

飞书开发入口见 [`docs/integrations/feishu/wiki.md`](../integrations/feishu/wiki.md)。

## `feat/eval_workbench_v1.0`：当前评测开发线

- 最新状态：2026-08-05 合并 `upstream/main` 后继续开发，当前与上游无落后。
- 分支独有的非 merge 提交共 72 个；总领先 98 个提交，其中包含来自 `preview` 的集成历史和上游合并提交。
- 主线能力：Phase 0–6 的评测工作台、评估集版本化、Run 状态机与双路径录制、自建指标、RAGAS 服务化评分、同版本 A/B 对比、配置快照和单样本重跑。
- 当前最新提交将评测配置前缀统一为 `ragent.eval`，并继续优化批处理和前端加载。

近期分支独有提交：

| 提交 | 日期 | 说明 |
|---|---|---|
| `8332f6ad` | 2026-08-05 | 精简评测台并优化批处理与前端加载 |
| `296b5142` | 2026-08-05 | 统一评测配置前缀为 `ragent.eval` 并处理合并冲突 |
| `f1c13961` | 2026-08-03 | 配置快照、RAGAS 创建选项与单样本重跑 |
| `93310f79` | 2026-07-31 | 同版本 Run A/B 对比 |
| `a507c059` | 2026-07-31 | 接入 RAGAS 评分服务 |

评测设计、契约与阶段报告见 [`docs/evaluation/README.md`](../evaluation/README.md)。

## 维护建议

1. 同步上游前，先刷新远程引用，再重算本页的 ahead/behind；本页数值是 2026-08-05 快照。
2. 合并到 `main` 前，应从 `preview` 和评测分支分别确认哪些功能及其数据库升级脚本需要保留。
3. 飞书、MCP、评测工作台各有外部依赖；合并后应分别验证凭证 / 服务连通性、数据库升级与关键链路。
